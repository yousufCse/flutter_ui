import 'package:flutter/material.dart';

class SettingsTab extends StatelessWidget {
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Change yours setting here!'),
      ),
    );
  }
}
