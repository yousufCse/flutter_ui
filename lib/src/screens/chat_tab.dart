import 'package:flutter/material.dart';

class ChatTab extends StatelessWidget {
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('No recent chats here'),
      ),
    );
  }
}
