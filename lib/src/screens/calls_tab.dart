import 'package:flutter/material.dart';

class CallsTab extends StatelessWidget {
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('No recent calls here'),
      ),
    );
  }
}
