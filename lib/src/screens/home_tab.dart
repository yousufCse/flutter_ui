import 'package:flutter/material.dart';

class HomeTab extends StatelessWidget {
  Widget build(BuildContext context) {
    return Container(
      color: Colors.grey[300],
      child: Center(
        child: Text('Good afternoon, Yousuf'),
      ),
    );
  }
}
